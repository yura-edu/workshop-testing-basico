const {
  isValidEmail,
  isStrongPassword,
  isPositiveInteger,
  isNonEmptyString,
  isISODate,
  isHttpsUrl,
} = require('./validators')

// ── Write your tests below ─────────────────────────────────────────────────
// Use describe/it/expect blocks. Cover happy paths, sad paths, and edge cases.

describe('isValidEmail', () => {
  // TODO: write tests
})

describe('isStrongPassword', () => {
  // TODO: write tests
})

describe('isPositiveInteger', () => {
  // TODO: write tests
})

describe('isNonEmptyString', () => {
  // TODO: write tests
})

describe('isISODate', () => {
  // TODO: write tests
})

describe('isHttpsUrl', () => {
  // TODO: write tests
})
